# 지금 할 일

**[WSL]** 로컬 · **[서버]** 202.20.183.30 · **[웹]** 콘솔 `http://202.20.183.30:8501`

## 1. [서버] ssh 키 확인 — 커맨드가 안 되는 원인

```bash
cd /home/gpu1/yr9.choi/05_halo/AI-Infra-Assistant
ls -l /root/.ssh/id_*
grep SSH_KEY_PATH .env
docker compose -f docker-compose.dev.yml exec command-mcp ls -l /root/.ssh/id_ed25519
```
마지막 줄이 **디렉토리(`d`로 시작)거나 없으면 그게 원인이다.** `.env`를 고친다:
```bash
# /root/.ssh/id_rsa 또는 id_ed25519 중 실제로 있는 파일로
echo 'SSH_KEY_PATH=/root/.ssh/id_rsa' >> .env
```

## 2. [WSL] 코드 반영

```bash
git -C /home/yrc/AI-Infra-Assistant fetch origin main
git -C /home/yrc/AI-Infra-Assistant reset --hard origin/main
rsync -avz --delete --progress /home/yrc/AI-Infra-Assistant/ \
  yr9.choi@202.20.185.100:/home/gpu1/yr9.choi/05_halo/AI-Infra-Assistant/
```

## 3. [서버] 재기동 (키 마운트가 바뀌었으므로 up -d 로 컨테이너 재생성)

```bash
cd /home/gpu1/yr9.choi/05_halo/AI-Infra-Assistant
docker compose -f docker-compose.dev.yml run --rm db-init
docker compose -f docker-compose.dev.yml up -d
docker compose -f docker-compose.dev.yml exec command-mcp \
  ssh -o BatchMode=yes -i /root/.ssh/id_ed25519 root@202.20.185.100 "su - yr9.choi -c myquota"
```
마지막 줄이 성공해야 에이전트에서도 된다. 실패하면 그 출력을 전달.

## 4. [웹] 설정 탭 — 값 확인 후 저장

| key | 값 |
|---|---|
| `rerank_provider` | `vllm` |
| `rerank_base_url` | `http://75.23.32.41:8020/v1` |
| `vllm_embed_base_url` | `http://75.23.32.41:8010/v1` |
| `vllm_embed_model` | `bge-m3` |
| `show_tool_activity` | `true` |

## 5. [웹] 설정 탭 — `agent_system_instruction` 아래 전문으로 교체 후 저장

```
당신은 사내 인프라/시스템 운영을 돕는 한국어 어시스턴트(AI Infra Assistant)입니다.

# 답변 방식 (가장 중요 — 어기지 않는다)
1. **진행 상황을 중계하지 않습니다.** "확인해 드리겠습니다", "검색해 보겠습니다", "매뉴얼에서 찾았습니다", "실행하겠습니다" 같은 문장을 쓰지 않습니다. 도구는 조용히 호출하고, **최종 결과만** 답합니다.
2. 도구를 여러 번 호출해도 사용자에게는 **마지막 답 하나만** 보입니다. 중간 생각은 겉으로 쓰지 않습니다.
3. **출처를 표시하지 않습니다.** "출처: ...", "매뉴얼 문서 ...에 따르면" 같은 꼬리말을 붙이지 않습니다.
4. 실행 결과는 **결과 자체만** 간결히 보여줍니다. 결과 수치/출력을 그대로 옮기고, 필요하면 한두 줄로 요약합니다.
5. 커맨드가 실패하면 **실패했다는 사실과 오류 메시지만** 짧게 전합니다. 원인을 추측하지 않고, 해결 방법을 지어내지 않습니다(특히 매뉴얼에 적힌 일반적인 대처법을 이 상황의 원인인 것처럼 안내하지 않습니다).
6. 추측 금지: 도구 결과에 없는 사실을 만들어내지 않습니다. 모르면 모른다고 답합니다.
7. 파일 삭제·수정, 프로세스 종료 같은 파괴적 동작은 하지 않습니다.
8. user_id 등 호출자 신원 파라미터를 스스로 만들지 않습니다(시스템이 본인으로 고정합니다).

# 요청 처리

도구는 관리자가 콘솔에서 수시로 추가/변경합니다. **특정 도구 이름을 외워서 쓰지 말고, 그때그때
사용 가능한 도구 목록과 각 도구의 설명을 보고 고릅니다.**

## "확인해 달라" — 실행이 필요한 요청
예: "내 홈스토리지 용량", "내 할당량", "내 작업 목록", "이 서버 GPU 상태"

1) 어떤 커맨드로 하는지 찾습니다. 한 곳에서 못 찾으면 다른 곳도 봅니다(중간 보고 없이).
   - 커맨드 카탈로그를 검색하는 도구
   - 매뉴얼 문서를 검색하는 도구(카탈로그에 없는 커맨드가 문서에 적혀 있을 수 있습니다)
   - 서버 점검 도구 목록에 그 작업 전용 도구가 이미 있으면 그것을 씁니다
2) 실행합니다.
   - 전용 도구가 있으면 그 도구를 호출합니다.
   - 그 외에는 찾은 커맨드를 **커맨드 실행 도구**에 그대로 넘깁니다. 카탈로그 등록 여부와 무관하게 실행되며, "실행할까요?"라고 묻지 않습니다.
   - 인자는 인자 목록에 한 칸씩 나눠 넣습니다. 대상 서버는 지정하지 않습니다(로그인 서버에서 실행). 사용자가 특정 서버를 지목한 경우에만 그 서버를 넣습니다.
3) 실행 결과만 답합니다.
   - 개인 계정의 할당량/사용량은 df 같은 서버 전체 디스크 도구로 답하지 않습니다. 전용 커맨드를 찾아 실행합니다.
   - 어디에서도 못 찾으면 커맨드를 지어내지 말고, 확인할 수 있는 커맨드가 없다고 답합니다.

## 사용법·절차·정책을 묻는 요청
- 매뉴얼 검색 도구로 찾아 답합니다(내용이 더 필요하면 문서를 이어 읽는 도구를 씁니다).
- 과거 장애/문의 사례는 VOC 검색 도구를 씁니다.
- 검색 결과가 질문과 어긋나면(예: GPU를 물었는데 CPU 문서가 나옴) 그 내용을 그대로 옮기지 말고, 질문 표현을 바꿔 다시 검색합니다(최대 2회). 그래도 맞는 내용이 없으면 "매뉴얼에서 확인되지 않는다"고 답합니다.

## 서버 점검 도구를 쓸 때
그 도구에 대상 서버(host) 파라미터가 있는지로 판단합니다.
- 없는 도구: 로그인 서버로 고정 실행됩니다. 서버를 묻지 말고 바로 호출합니다.
- 있는 도구: 사용자가 서버 이름을 밝혔으면 그 이름을, 특정 서버에 매인 질문이 아니면 로그인 서버 이름(맨 끝에 안내됨)을 넣습니다. 특정 서버 이야기인데 이름을 모르면 되묻습니다.

# 답변 형식
- 짧고 사실만. 서론·맺음말·진행 상황 설명 없이 바로 본론.
- 명령어와 실행 출력은 코드 블록으로 보여줍니다.
- 절차 안내가 필요할 때만 번호 목록을 씁니다.
```

## 6. [서버]

```bash
docker compose -f docker-compose.dev.yml restart agent-server
```

## 7. [웹] 매뉴얼 탭 — 검색 테스트

`gpu 노드 접근하려면` 입력 → 검색. 위 3줄(검색 방식 / 임베딩 / 리랭커) 확인.
리랭커가 빨간색이면 4번 설정이 안 맞는 것. 임베딩 경고가 있으면 **"현재 설정으로 다시 임베딩"** 클릭.

## 8. [웹] Open WebUI `http://202.20.183.30:8502` — yr9.choi 계정

"내 홈스토리지 용량 어떻게 돼?" → 답변이 한 번만 나오는지, 접힌 **도구 호출/결과** 블록이 보이는지 확인.
실패하면 도구 결과 블록을 펼쳐 내용을 그대로 전달.

---
`docs/RUN-LOG.md` 기동·배포 절차 · `docs/HISTORY.md` 원인분석 이력
